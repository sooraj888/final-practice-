import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const NavBar = (): JSX.Element => {
  return (
    <AppBar position="static">
      <Toolbar>Post List App</Toolbar>
    </AppBar>
  );
};

export default NavBar;
